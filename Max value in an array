//WAP to find max value in an array 

#include<stdio.h>
int main()
{	int arr[5] = {5,9,15,1,8};
	int max = arr[0];
	  for(int i=1; i<5; i++)
		if(max < arr[i]){
			max = arr[i];
		}
		printf("Maximum value of given array is %d",max);
  return 0;
}
